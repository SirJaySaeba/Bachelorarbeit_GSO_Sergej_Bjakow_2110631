<%= form_for(current_user.gamerelations.find_by_followed_id(@game),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Entfernen", class: "btn btn-large" %>
<% end %>